websocket and html5 based video chat app in the works

to install

* ```npm install -g bower```
* ```npm install -g gulp```
* ```npm install```
* ```bower install```

to test
* ```gulp test```

to start server for development
* ```gulp develop```

to start server
* ```npm start```